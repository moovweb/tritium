* Parser
  * Implement Types
    * Listed in the spec.
    * Any first letter capitalized, unquoted, A-z term
  * Regexp
    * Allow us to define regexp with the ` character
    * Aka: `http\\/\//\\\\///`
* Engines
  * match(), with(), not(), and select() should return either "true" or "false"