# Remove all scripts for now except the ones needed for jquery
# $("//script[not (contains(@src, 'my-matches.js') or contains(@src, 'match-profile.js') or contains(@src, 'jquery'))]") {
#   remove()
# }
# Remove the original page content
# $("/html/body/corpse") {
#   remove()
# }
