#sessions
  - @sessions.each do |session|
    .session
      = session.created_at
      %a{:href => "/sessions/#{session.id}"}= session.path

- if @session
  #steps
    %h2 Slowest Requests
    %ul#slowest
      - @session.slowest_searches.each do |step|
        %li
          = step.instruction.stub
          (took #{step.search_time_ns / 1000000.0} ms)
    %h2 Trace
    = haml :steps