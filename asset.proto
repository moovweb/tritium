package hermes;

/* 
 * I'm not sure what assets we need in a module beyond those contained in the recipes. 
 * 
 * Perhaps examples / documentation?
 */

message Asset {
  repeated string name = 1;
  repeated string path = 2;
  repeated string data = 3;
}
